# 警告・障害通知の共通ルール

今後このリポジトリで追加・変更する警告は、読む人が技術用語を知らなくても「放置してよいか」「こちらで何をするか」を判断できる形にします。

## 必ず書く2項目

1. **何が起きた** — 対象と確認できた事実を、1〜2文で平易に書く
2. **おまえがやること** — 「何もしなくてOK」または具体的な1手だけを書く

影響や自動復旧の説明は、ユーザーの行動判断が変わる場合だけ上の2項目へ短く含めます。リポジトリ名、ワークフロー名、英語の例外などの技術情報は詳細リンクや折りたたみへ回します。

## 重要度

- 🟠 **様子見**: 1回の失敗、通常より少し長い遅延、状態取得の一時失敗。原則として自動再確認し、すぐの操作は求めない
- 🔴 **対応が必要**: 連続2回以上の失敗、許容時間の2倍を超える停止、ワークフロー無効化、成功実績なし。通知をそのまま保守担当へ渡す
- 🟢 **復旧**: 正常終了を確認済み。ユーザー操作は不要

## Discordに送る条件

- Discordへ送るのは、ユーザーの対応が必要な赤い警告だけ
- 予備の監視番だけが遅れ、実処理を担う本体やリレーが正常なら、Issueへ記録して自動再確認するがDiscordへは送らない
- 様子見、一時的な取得失敗、復旧はDiscordへ送らず、Issueへの記録と自動確認だけ行う
- 同じ未解決事象は初回だけDiscordへ送る
- 監視対象の異常と、監視番自身の故障を別の事象として扱う
- 正常な定期実行の遅れを考慮し、実行間隔ぎりぎりのしきい値にしない

## 文面例

```text
🔴 対応が必要：〇〇監視が2回連続で失敗しました

何が起きた
直近の実行が2回連続で失敗しました。

おまえがやること
この通知をそのままわたしに送ってください。
```
