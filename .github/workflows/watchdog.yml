name: MixChannel 停止監視（統合先へ移行済み）
on:
  workflow_dispatch:
    inputs:
      dry_run:
        type: boolean
        default: true
jobs:
  migrated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Moved to ranking-watchdog.yml in mixch-archive-monitor-public'
