name: MixChannel勢い監視（統合先へ移行済み）
on:
  workflow_dispatch:
    inputs:
      dry_run:
        type: boolean
        default: true
      test_webhook:
        type: boolean
        default: false
      threshold_override:
        type: string
        required: false
jobs:
  migrated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Moved to nekoromme/mixch-archive-monitor-public'
